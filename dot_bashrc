source ~/.bash_exports
source ~/.bash_alias
source ~/.bash_ssh

fastfetch

set -o vi

if [[ $(which starship) ]]; then
  eval "$(starship init bash)"
fi

if [[ $(which zoxide) ]]; then
  eval "$(zoxide init bash)"
fi
